# Version-bounded dev dependencies (ranges, not exact pins).
# For reproducible grading: pip install -r requirements-lock.txt && pip install -e .
# Regenerate lockfile (Python 3.10 or 3.12, matches CI):
#   python -m piptools compile requirements-dev.txt -o requirements-lock.txt --strip-extras
cryptography>=42.0.0,<45.0.0
pytest>=8.3.0,<9.0.0
pytest-cov>=5.0.0,<7.0.0
hypothesis>=6.100.0,<7.0.0
ruff>=0.8.0,<1.0.0
mypy>=1.13.0,<2.0.0
pre-commit>=3.8.0,<5.0.0
